<?php

use Modularity\Tests\FeatureTestCase;
use Modularity\Tests\HttpTestCase;
use Modularity\Tests\TestCase;

uses(TestCase::class)->in('Unit');
uses(FeatureTestCase::class)->in('Feature');
uses(HttpTestCase::class)->in('Http');
